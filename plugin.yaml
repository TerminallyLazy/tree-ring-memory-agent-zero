name: tree_ring_memory
title: Tree Ring Memory
description: Agent Zero bridge to the pending Rust-native Tree Ring Memory v0.14 CLI protocol; not releasable until matching core artifacts exist.
version: 3.1.0
settings_sections:
  - agent
  - developer
per_project_config: true
per_agent_config: true
